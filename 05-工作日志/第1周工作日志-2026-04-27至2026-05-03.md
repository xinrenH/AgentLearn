# 第1周工作日志（2026-04-27 至 2026-05-03）

## 本周定位
- 阶段：第1阶段（基础补齐）
- 周主题：工程基础与 API 能力
- 周目标：完成一个可运行的 Agent 基础后端骨架，具备用户、任务、日志接口，接入数据库与缓存，具备基础错误处理与日志追踪能力。

## 本周目标与验收标准
1. 完成 FastAPI 项目初始化与基础目录结构。
2. 完成 3 组核心接口：用户、任务、日志。
3. 完成 PostgreSQL 接入与基础 CRUD。
4. 完成 Redis 接入与任务状态缓存。
5. 完成统一异常处理与请求日志。
6. 完成 README、启动说明、接口说明。

验收标准：
- 本地可启动并可访问接口文档。
- 核心接口自测通过。
- 关键错误可定位到具体请求与模块。

---

## 每日详细记录

## Day1（周一，2026-04-27）
### 学习主题
- Python 异步基础、类型标注、FastAPI 项目初始化规范。

### 执行任务
1. 创建学习仓库与基础目录：`app/`、`api/`、`core/`、`tests/`、`docs/`。
2. 初始化依赖与运行入口，验证 `uvicorn` 启动。
3. 建立配置文件分层（开发/测试）。
4. 初步设计模块边界：接口层、服务层、存储层。

### 实际执行明细
1. 完成目录初始化：`D:\AIAgent\AgentLearn\Demo1` 下创建 `app/`、`api/`、`core/`、`tests/`、`docs/`。
2. 完成环境与依赖：
   - 创建虚拟环境：`py -m venv .venv`
   - 激活环境：`.venv\Scripts\Activate.ps1`
   - 安装依赖：`pip install fastapi "uvicorn[standard]" pydantic-settings`
   - 锁定依赖：`pip freeze > requirements.txt`
3. 完成运行入口：
   - 新建 `app/main.py`
   - 暴露 `/health` 健康检查
   - 使用 `uvicorn app.main:app --reload --host 127.0.0.1 --port 8000` 启动
4. 完成配置分层：
   - 新建 `core/config.py`
   - 新建 `.env.dev`、`.env.test`
   - 接入环境加载逻辑，按 `APP_ENV` 切换开发/测试配置
5. 完成模块边界落地（骨架版）：
   - 路由层：`api/router.py` + `api/routes/user.py`、`task.py`、`log.py`
   - 服务层：`app/services/user_service.py`、`task_service.py`、`log_service.py`
   - 存储层：`app/repositories/memory.py`（内存仓储）
   - 主入口挂载：`app/main.py` 统一挂载 `/api/*`

### 当日验收结果
1. `GET /health` 可访问，返回服务状态。
2. `GET /docs` 可访问，Swagger 页面可打开。
3. `users/tasks/logs` 三组接口已注册到 `/api` 前缀下。

### 当日报错与修复记录
1. 报错：`ModuleNotFoundError: No module named 'app.repositories.memory_repo'`
   - 原因：实际文件名为 `memory.py`，导入路径写成了 `memory_repo`。
   - 修复：统一改为 `from app.repositories.memory import MemoryRepo`。
2. 报错：`log_service.py` 误写成 `TaskService` 内容。
   - 原因：复制代码时未替换完整。
   - 修复：重写 `log_service.py`，恢复 `LogService.add_log/list_logs`。
3. 报错：`ValidationError: debug ... input_value='release'`
   - 原因：系统环境变量 `DEBUG=release` 污染配置解析。
   - 修复：配置改为 `APP_` 前缀（`APP_NAME/APP_ENV/APP_DEBUG/APP_API_PREFIX`），隔离系统同名变量。

### 当日产出
1. 可运行的最小 FastAPI 服务（含健康检查和文档页）。
2. 开发/测试环境配置分层能力。
3. 三层结构（接口层、服务层、存储层）骨架代码。
4. 可复用依赖文件：`requirements.txt`。

### 复盘
- 完成度：100%（Day1 计划项已全部落地并可启动）。
- 遗留项：`tests` 示例用例尚未补齐（放到 Day2/Day3 跟进）。
- 次日重点：完善业务接口行为约束（状态枚举、统一响应结构、异常码规范）。

---


## Day2（周二，2026-04-28）
### 学习主题
- FastAPI 路由组织、依赖注入、请求参数校验。

### 执行任务
1. 设计并实现用户接口：创建用户、查询用户。
2. 设计并实现任务接口：创建任务、查询任务、更新任务状态。
3. 设计并实现日志接口：写入日志、按条件查询日志。
4. 建立统一响应格式与错误码定义。

### 当日产出
- 3 组接口可在 Swagger 中可见并可调用。
- 参数校验规则生效（必填、格式、长度）。

### 问题与处理
- 问题：响应结构不一致，前端/调用方对接成本高。
- 处理：新增统一响应模型，所有接口返回同一结构体。

### 复盘
- 完成度：95%
- 未完成：任务接口的部分边界状态未加限制。
- 次日重点：接入数据库并落地持久化。

---

## Day3（周三，2026-04-29）
### 学习主题
- PostgreSQL 接入、ORM 基础、事务控制。

### 执行任务
1. 配置 PostgreSQL 连接池与会话管理。
2. 建立用户、任务、日志三张基础表模型。
3. 将接口从内存数据改为数据库持久化。
4. 完成基础 CRUD 自测。

### 当日产出
- 数据落库链路打通。
- 用户/任务/日志核心接口完成数据库化。

### 问题与处理
- 问题：并发情况下任务状态更新存在覆盖风险。
- 处理：在更新逻辑中加入版本校验/条件更新，避免误覆盖。

### 复盘
- 完成度：85%
- 未完成：查询性能尚未优化，分页字段不完整。
- 次日重点：补缓存与任务状态读取性能优化。

---

## Day4（周四，2026-04-30）
### 学习主题
- Redis 缓存策略、缓存一致性、过期策略。

### 执行任务
1. 接入 Redis 客户端，封装缓存读写工具。
2. 为任务状态查询增加缓存层，设置合理 TTL。
3. 实现缓存失效策略（任务更新后主动删除旧缓存）。
4. 记录缓存命中率与回源次数。

### 当日产出
- 任务状态接口具备缓存能力。
- 初版缓存策略说明文档。

### 问题与处理
- 问题：更新后短时间读取到旧值。
- 处理：更新操作后立刻清理缓存并写入最新值，降低脏读概率。

### 复盘
- 完成度：90%
- 未完成：缓存异常场景降级逻辑还需细化。
- 次日重点：做异常处理、日志追踪、统一中间件。

---

## Day5（周五，2026-05-01）
### 学习主题
- 全局异常处理、请求链路日志、可观测性基础。

### 执行任务
1. 增加全局异常处理中间件，统一错误输出。
2. 增加请求日志中间件（请求ID、耗时、状态码）。
3. 对关键接口增加业务日志（任务创建、状态变更）。
4. 补充常见异常场景测试（参数错误、资源不存在、数据库异常）。

### 当日产出
- 可追踪请求链路，错误定位更快。
- 日志包含关键排障字段。

### 问题与处理
- 问题：日志过多影响排查效率。
- 处理：按级别分层（INFO/WARN/ERROR），并约束日志模板。

### 复盘
- 完成度：92%
- 未完成：告警机制未接入（留到后续监控阶段）。
- 次日重点：文档收尾、部署演示、周总结。

---

## Day6（周六，2026-05-02）
### 学习主题
- 工程交付文档化、最小部署流程。

### 执行任务
1. 完成 README（安装、启动、环境变量、接口调试）。
2. 输出接口说明（主要请求/响应示例）。
3. 补充项目结构说明与模块职责。
4. 执行一次从空目录到可运行的完整启动演练。

### 当日产出
- 新环境可按文档启动服务。
- 文档具备基础可交付性。

### 问题与处理
- 问题：文档中部分步骤依赖隐式知识（例如本地数据库准备）。
- 处理：补充前置依赖清单与检查命令。

### 复盘
- 完成度：95%
- 未完成：自动化脚本（一键初始化）暂未完成。
- 次日重点：周复盘与下周任务拆解。

---

## Day7（周日，2026-05-03）
### 学习主题
- 周复盘方法、下周计划拆解。

### 执行任务
1. 汇总本周完成项与未完成项。
2. 统计本周关键数据：接口完成率、问题关闭率、文档完整度。
3. 制定第2周计划（LLM调用、提示词模板、结构化输出）。
4. 输出风险清单与应对策略。

### 当日产出
- 本周复盘文档。
- 第2周执行清单（按天）。

### 问题与处理
- 问题：第2周内容涉及模型调用，可能受 API 稳定性影响。
- 处理：预设 mock 通道，先保障流程开发不阻塞。

### 复盘
- 完成度：100%（复盘任务本身）
- 下周关键动作：先搭模型调用封装，再做 Prompt 模板库。

---

## 本周总体总结

## 完成情况
1. 已完成：基础项目结构、3组API、数据库接入、缓存接入、异常处理、日志追踪、基础文档。
2. 部分完成：边界状态校验、缓存异常降级、自动化初始化脚本。
3. 未完成：监控告警接入（计划在第10周处理）。

## 关键指标（周末口径）
- 周目标完成率：约 90%
- 核心接口可用率（自测）：> 95%
- 主要问题关闭率：> 85%
- 文档完备度：约 85%

## 本周主要风险
1. 接口异常场景覆盖还不够全面。
2. 缓存策略在高并发场景下仍需压测验证。
3. 自动化程度偏低，重复初始化成本较高。

## 已确定改进项（下周执行）
1. 完成模型调用封装，并提供重试与超时控制。
2. 建立 Prompt 模板版本管理（V1/V2 对比）。
3. 增加结构化输出校验与失败回退逻辑。
4. 补充 10 条注入攻击样例做基线测试。

## 下周（第2周）执行清单
1. 完成 LLM 基础调用层。
2. 完成 3 类提示词模板（问答/抽取/改写）。
3. 完成 JSON 结构化输出 + 校验。
4. 输出 Prompt 对比报告与失败案例归档。

---

## 更新记录
- 2026-04-27：首次创建第1周详细工作日志。
- 2026-04-27：补充 Day1 完整执行记录、验收结果与报错修复明细。
